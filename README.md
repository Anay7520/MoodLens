# MoodLens
under 350 character  MoodLens is a sentiment analysis app that helps users understand the mood behind their text. It uses transformer models to detect sentiment, with a Streamlit frontend, FastAPI backend secured by JWT, and MongoDB for data storage.
